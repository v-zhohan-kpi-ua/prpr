/** This is also an "array", which tells
 * in which order steps should be
 * and it used in translations of the steps. */
export enum Steps {
  Personal = "personal",
  DamagedProperty = "damagedProperty",
  Docs = "docs",
  Confirmation = "confirmation",
}
